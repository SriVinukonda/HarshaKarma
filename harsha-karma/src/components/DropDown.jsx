function Button(props) {
    // Send the title name in item props
  return (
    
    <div className='buttonBody'>
        {props.buttonTitle}
    </div>
  );
}
// test commit

export default Button;
